version: "3.8"

services:
  web:
    image: nginx:1.27-alpine
    container_name: whatsapp-only-ecommerce
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./:/usr/share/nginx/html:ro
